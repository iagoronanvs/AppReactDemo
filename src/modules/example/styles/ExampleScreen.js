import { StyleSheet } from 'react-native';

const styleSheet = StyleSheet.create({
    container: {
        width: '100%',
        padding: 20,
        position: 'absolute',
        bottom: 0
    },

});

export default styleSheet;