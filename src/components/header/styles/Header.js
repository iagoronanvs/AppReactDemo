import { StyleSheet } from 'react-native';

const styleSheet = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 10
    },
    logo: {
        width: 40,
        height: 40
    }
});

export default styleSheet;