import { StyleSheet } from 'react-native';

const styleSheet = StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: 10
    },
    title: {
        fontSize: 22,
        fontWeight: 'bold',
        marginBottom: 10
    },
    subtitle: {
        fontSize: 18
    }
});

export default styleSheet;