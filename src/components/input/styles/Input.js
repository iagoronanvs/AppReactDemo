import { StyleSheet } from 'react-native';

const styleSheet = StyleSheet.create({
    conatiner: {
        marginBottom: 10
    },
    default: {
        marginBottom: -10
    },
    border: {
        borderWidth: 1,
        borderColor: 'black',
        borderRadius: 15,
        paddingTop: 5,
        paddingLeft: 10,
    },
});

export default styleSheet;