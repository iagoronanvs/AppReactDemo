import { StyleSheet } from 'react-native';

const styleSheet = StyleSheet.create({
    container: {
        padding: 20,
        flexDirection: 'row'
    },
    info: {
        marginLeft: 10
    },
    name: {
        fontWeight: 'bold'
    },
    image: {
        width: 70,
        height: 70,
        borderRadius: 50
    }
});

export default styleSheet;